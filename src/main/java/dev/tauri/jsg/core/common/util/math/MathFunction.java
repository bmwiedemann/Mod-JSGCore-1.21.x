package dev.tauri.jsg.core.common.util.math;

public interface MathFunction {

	/**
	 * Calculates the function's values for a given argument.
	 * 
	 * @param x Argument
	 * @return Value
	 */
	float apply(float x);
}
